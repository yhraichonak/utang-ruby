
Before do
    @About_Window = About_Window.new @selenium
    @Header_Bar = Header_Bar.new @selenium
  end
  
  When(/^I ssh to the "(.*?)" server$/) do | server |
    
  end

  When(/^I cd to the  "(.*?)" directory$/) do | directory |
    
  end
  
  When(/^I cd to the "(.*?)" folder$/) do | folder |
    
  end
  
  When(/^I vim config file "(.*?)" and i set "(.*?)" to "(.*?)"$/) do | config_type, config_name, config_value |
    
  end
  